PORT=5001
CLIENT_URL=http://localhost:5173
JSON_LIMIT=8mb
TOKEN_SECRET=replace-with-a-secure-random-string
CONNECTION_STRING=mongodb://127.0.0.1:27017/chat-app
CLOUD_NAME=your-cloudinary-cloud-name
API_KEY=your-cloudinary-api-key
API_SECRET=your-cloudinary-api-secret
# For the AI assistant features (summarize / suggest reply). Free key from
# https://console.groq.com. Server-side only — never expose to the frontend.
GROQ_API_KEY=your-groq-api-key
